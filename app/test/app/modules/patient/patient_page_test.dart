import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:dentistry/app/modules/patient/patient_page.dart';

main() {
  testWidgets('PatientPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PatientPage(title: 'Patient')));
    //  final titleFinder = find.text('Patient');
    //  expect(titleFinder, findsOneWidget);
  });
}
