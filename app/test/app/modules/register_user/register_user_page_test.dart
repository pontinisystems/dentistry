import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:dentistry/app/modules/register_user/register_user_page.dart';

main() {
  testWidgets('RegisterUserPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RegisterUserPage(title: 'RegisterUser')));
    //  final titleFinder = find.text('RegisterUser');
    //  expect(titleFinder, findsOneWidget);
  });
}
