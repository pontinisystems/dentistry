import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:dentistry/app/modules/register_patient/register_patient_page.dart';

main() {
  testWidgets('RegisterPatientPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(RegisterPatientPage(title: 'RegisterPatient')));
    //  final titleFinder = find.text('RegisterPatient');
    //  expect(titleFinder, findsOneWidget);
  });
}
